---
- hosts: mfcloud

  roles:
    - { role: ribozz.mfcloud }
    - { role: ribozz.datadog }

  tasks:
    - name: Install common packages
      apt: name={{ item }}
      with_items:
        - vim
        - htop

